// Kept as the path the coming-soon page already imports. The artwork itself
// now lives in components/ui/Logo.tsx, alongside the wordmark and the lockups.
export { OutriderMark as default } from "./ui/Logo";
